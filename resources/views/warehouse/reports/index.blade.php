@extends('layouts.warehouse')
@section('title', 'Báo cáo kho theo ngày')
@section('content')
<style>
.report-hero{background:linear-gradient(135deg,#0f766e,#164e63);color:#fff;border-radius:16px;padding:24px;margin-bottom:20px}.report-hero h1{font-size:1.65rem;font-weight:750;margin:0 0 4px}.report-panel,.metric{background:#fff;border:1px solid #e8edf2;border-radius:14px;box-shadow:0 2px 10px rgba(15,23,42,.04)}.report-panel{margin-bottom:20px;overflow:hidden}.report-title{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:16px 18px;border-bottom:1px solid #e8edf2}.report-title h2{font-size:1.05rem;font-weight:750;margin:0}.metric{padding:16px;height:100%}.metric .value{color:#0f766e;font-size:1.55rem;line-height:1.15;font-weight:800}.metric .label{color:#64748b;font-size:.82rem;margin-top:6px}.report-table{margin:0;min-width:820px}.report-table th{background:#f8fafc;color:#475569;font-size:.76rem;text-transform:uppercase;white-space:nowrap}.report-table td,.report-table th{padding:11px 14px;vertical-align:middle;border-color:#edf1f5}.empty-state{color:#64748b;text-align:center;padding:34px 16px!important}.money{white-space:nowrap}
</style>
<div class="report-hero"><h1><i class="bi bi-clipboard-data me-2"></i>Một ngày làm việc — {{ $warehouse?->name ?? 'Toàn hệ thống' }}</h1><div class="opacity-75">Chỉ hiển thị hoạt động thuộc kho đang quản lý</div></div>
<div class="report-panel p-3"><form method="GET" class="row g-3 align-items-end"><div class="col-md-4"><label class="form-label fw-semibold">Ngày làm việc</label><input type="date" name="selected_date" class="form-control" value="{{ $selectedDate }}"></div><div class="col-md-3"><button class="btn btn-primary w-100"><i class="bi bi-funnel me-1"></i>Xem báo cáo</button></div></form></div>
<div class="row g-3 mb-4">
<div class="col-6 col-xl-3"><div class="metric"><div class="value">{{ number_format($totals['on_hand'],0,',','.') }}</div><div class="label">Tổng thể ({{ $warehouse?->name ?? 'toàn hệ thống' }}) — tồn hiện tại</div></div></div>
<div class="col-6 col-xl-3"><div class="metric"><div class="value">{{ number_format($totals['transfer_quantity'],0,',','.') }}</div><div class="label">Hàng nhà máy lên</div></div></div>
<div class="col-6 col-xl-3"><div class="metric"><div class="value">{{ number_format($totals['supplier_quantity'],0,',','.') }}</div><div class="label">Hàng San Hà</div></div></div>
<div class="col-6 col-xl-3"><div class="metric"><div class="value">{{ number_format($exportedOrders->sum(fn($row) => $row['document']->items->sum('quantity')),0,',','.') }} <small class="fs-6">/ {{ $exportedOrders->count() }} phiếu</small></div><div class="label">Hàng + Đơn hàng xuất đi</div></div></div>
</div>
<section class="report-panel" id="factory-incoming"><div class="report-title"><h2>1. Hàng nhà máy lên</h2><span class="badge bg-info text-dark">{{ $inventoryTransfers->count()+$orderTransfers->count() }} lượt</span></div><div class="table-responsive"><table class="table report-table"><thead><tr><th>Ngày nhận</th><th>Mã</th><th>Nguồn gửi</th><th>Loại</th><th class="text-end">Số lượng</th></tr></thead><tbody>
@foreach($inventoryTransfers as $transfer)<tr><td>{{ $transfer->received_at?->format('d/m/Y H:i') }}</td><td>{{ $transfer->transfer_code }}</td><td>{{ $transfer->sourceWarehouse?->name??'—' }}</td><td>Hàng điều chuyển</td><td class="text-end fw-bold">{{ number_format($transfer->items->sum('quantity'),0,',','.') }}</td></tr>@endforeach
@foreach($orderTransfers as $transfer) @php($items=$transfer->importDocument?->items??collect())<tr><td>{{ $transfer->received_at?->format('d/m/Y H:i') }}</td><td>#{{ $transfer->order?->code??$transfer->order_id }}</td><td>{{ $transfer->sourceWarehouse?->name??'—' }}</td><td>Đơn điều chuyển</td><td class="text-end fw-bold">{{ number_format($items->isNotEmpty()?$items->sum('quantity'):($transfer->order?->items->sum('quantity')??0),0,',','.') }}</td></tr>@endforeach
@if($inventoryTransfers->isEmpty()&&$orderTransfers->isEmpty())<tr><td colspan="5" class="empty-state">Không có hàng nhà máy lên trong ngày.</td></tr>@endif</tbody></table></div></section>
<section class="report-panel" id="supplier-imports"><div class="report-title"><h2>2. Hàng San Hà</h2><span class="badge bg-primary">{{ $supplierImports->count() }} phiếu</span></div><div class="table-responsive"><table class="table report-table"><thead><tr><th>Ngày / Phiếu</th><th>Sản phẩm</th><th>Size</th><th class="text-end">Số lượng</th><th class="text-end">Thành tiền</th></tr></thead><tbody>
@forelse($supplierImports as $document) @foreach($document->items as $item)<tr><td>@if($loop->first){{ $document->document_date?->format('d/m/Y') }}<small class="d-block text-muted">{{ $document->document_number }}</small>@endif</td><td>{{ $item->productVariant?->product?->name??'Sản phẩm đã xóa' }}</td><td>{{ $item->productVariant?->size?:($item->productVariant?->name??'—') }}</td><td class="text-end fw-bold">{{ number_format($item->quantity,0,',','.') }}</td><td class="text-end money">{{ number_format($item->quantity*$item->unit_cost,0,',','.') }} ₫</td></tr>@endforeach @empty<tr><td colspan="5" class="empty-state">Không có hàng San Hà nhập trong ngày.</td></tr>@endforelse</tbody></table></div></section>
<section class="report-panel" id="exported-orders"><div class="report-title"><h2>3. Hàng + Đơn hàng xuất đi</h2><span class="badge bg-danger">{{ $exportedOrders->count() }} phiếu</span></div><div class="table-responsive"><table class="table report-table"><thead><tr><th>Ngày / Phiếu</th><th>Mã đơn</th><th>Khách hàng</th><th>Danh sách hàng</th><th class="text-end">Tổng SL</th></tr></thead><tbody>
@forelse($exportedOrders as $row)<tr><td>{{ $row['document']->document_date?->format('d/m/Y') }}<small class="d-block text-muted">{{ $row['document']->document_number }}</small></td><td>{{ $row['order'] ? '#'.$row['order']->code : 'Xuất hàng' }}</td><td>{{ $row['order']?->customer?->name??'—' }}</td><td>@foreach($row['document']->items as $item)<div>{{ $item->productVariant?->product?->name }} — {{ $item->productVariant?->size?:$item->productVariant?->name }}: <strong>{{ number_format($item->quantity,0,',','.') }}</strong></div>@endforeach</td><td class="text-end fw-bold">{{ number_format($row['document']->items->sum('quantity'),0,',','.') }}</td></tr>@empty<tr><td colspan="5" class="empty-state">Không có hàng hoặc đơn hàng xuất đi trong ngày.</td></tr>@endforelse</tbody></table></div></section>
@endsection
